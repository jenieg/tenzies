const Game = () => {
    return (
        <main className="h-screen w-screen flex items-center justify-center bg-gray-900">
            <div className="h-[90vh] w-[90vw] rounded-xl bg-gray-100">
            </div>
        </main>
    );
};

export default Game;
